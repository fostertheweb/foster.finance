{
	"name": "foster-finance",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"web": "yarn workspace web start",
		"server": "yarn workspace server run develop",
		"build": "yarn workspaces run build",
		"build:web": "yarn web run build",
		"build:server": "yarn workspace server run build"
	},
	"workspaces": [
		"web",
		"server"
	],
	"private": true
}
