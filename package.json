{
  "name": "foster-finance",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "client": "yarn workspace web start",
    "server": "yarn workspace server run develop",
    "build": "yarn workspace web build && yarn workspace server run dist"
  },
  "workspaces": [
    "web",
    "server"
  ],
  "private": true
}
