#import "./plaid/transactions.graphql"

type ExchangePublicTokenResponse {
  access_token: String
  item_id: String
  request_id: String
  status_code: Int
}

type Query {
  getTransactions(input: TransactionsRequest): TransactionsResponse
  getAccounts: AccountsResponse
}

type Mutation {
  exchangePublicToken(token: String!): ExchangePublicTokenResponse
}
