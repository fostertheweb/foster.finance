#import "./bank.graphql"
#import "./recurring-expense.graphql"

type User {
  user_id: String!
  banks: [Bank]
  recurring_expenses: [RecurringExpenses]
  first_name: String
  last_name: String
  email: String
  emoji: String
}
