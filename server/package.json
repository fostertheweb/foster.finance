{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "develop": "nodemon",
    "start": "node -r dotenv/config src/server",
    "dist": "run-s dist:clean dist:copy dist:install dist:zip",
    "dist:clean": "rm -rf dist && rm -f lambda.zip",
    "dist:copy": "mkdir dist && cp -R src/** dist/ && cp package.json dist/",
    "dist:install": "cd dist; yarn install --production",
    "dist:zip": "cd dist; zip -r ../lambda.zip ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server-lambda": "^2.9.7",
    "graphql": "^14.5.7",
    "graphql-tag": "^2.10.1",
    "massive": "^6.1.1",
    "nexus": "^0.12.0-rc.4",
    "plaid": "^4.3.0"
  },
  "devDependencies": {
    "apollo-server": "^2.9.3",
    "cross-zip-cli": "^1.0.0",
    "dotenv": "^8.2.0",
    "nodemon": "^1.19.2",
    "npm-run-all": "^4.1.5"
  }
}
